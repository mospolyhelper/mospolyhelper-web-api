object Modules {
    object Data {
        private const val prefix = ":data"

        const val Base = "$prefix:base"
        const val Auth = "$prefix:auth"
        const val Schedule = "$prefix:schedule"
        const val Performance = "$prefix:performance"
        const val Peoples = "$prefix:peoples"
        const val Applications = "$prefix:applications"
        const val Payments = "$prefix:payments"
        const val Personal = "$prefix:personal"
    }

    object Domain {
        private const val prefix = ":domain"

        const val Base = "$prefix:base"
        const val Auth = "$prefix:auth"
        const val Schedule = "$prefix:schedule"
        const val Performance = "$prefix:performance"
        const val Peoples = "$prefix:peoples"
        const val Applications = "$prefix:applications"
        const val Payments = "$prefix:payments"
        const val Personal = "$prefix:personal"
    }

    object Features {
        private const val prefix = ":features"

        const val Base = "$prefix:base"
        const val Auth = "$prefix:auth"
        const val Schedule = "$prefix:schedule"
        const val Performance = "$prefix:performance"
        const val Peoples = "$prefix:peoples"
        const val Applications = "$prefix:applications"
        const val Payments = "$prefix:payments"
        const val Personal = "$prefix:personal"
    }
}